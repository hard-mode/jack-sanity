#!/usr/bin/env node
var nodemon = require('nodemon');
var fs = require('fs');
var app_dir = fs.realpathSync(__dirname + '/..');

nodemon({
	script:	app_dir + '/src/jack-sanity.js',
	cwd:	app_dir,
	ext:	'js json'
});